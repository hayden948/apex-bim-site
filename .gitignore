# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# image-slot runtime sidecar (only written by the design tool)
.image-slots.state.json

# console test dependencies (installed or symlinked locally; no trailing
# slash so a symlinked node_modules is ignored too)
tests/console/node_modules
